# =============================================================================
# requirements.txt — وابستگیهای اصلی runtime
# =============================================================================

# ---------- پردازش داده ----------
pandas>=2.0,<3.0

# ---------- پردازش زبان طبیعی ----------
nltk>=3.8,<4.0
# spaCy 3.8+ → thinc>=8.3.12 → فقط Python >=3.10
# برای پشتیبانی از Python 3.9 در CI/Docker: سری 3.7.x
spacy>=3.5,<3.8

# ---------- خواندن/نوشتن فایل اکسل ----------
openpyxl>=3.1,<4.0
